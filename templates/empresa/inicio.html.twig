{% extends 'empresa/base.html.twig' %}

{% block title %}Inicio - Empresa{% endblock %}

{% block content %}
<div class="container-fluid py-4 px-3">
    <div class="row g-3">
        
        <!-- Tarjeta Empleados -->
        <div class="col-12 col-md-6 d-flex align-items-center justify-content-center" style="height: calc(100vh - 40px);">
            <a href="{{ path('app_empresa_empleados_inicio') }}" class="text-decoration-none w-100 h-100">
                <div class="card text-center shadow-sm w-100 h-100 border-0" style="background-color: #eaf4ff; border-radius: 20px;">
                    <div class="card-body d-flex flex-column justify-content-center align-items-center">
                        <i class="bi bi-people-fill" style="font-size: 3rem; color: #3b82f6;"></i>
                        <h4 class="card-title fw-bold mt-3 text-dark">Empleados</h4>
                    </div>
                </div>
            </a>
        </div>

        <!-- Tarjeta Horario 
        <div class="col-12 col-md-6 d-flex align-items-center justify-content-center" style="height: calc(100vh - 40px);">
            <a href="{{ path('app_empresa_horario_inicio') }}" class="text-decoration-none w-100 h-100">
                <div class="card text-center shadow-sm w-100 h-100 border-0" style="background-color: #e0f7fa; border-radius: 20px;">
                    <div class="card-body d-flex flex-column justify-content-center align-items-center">
                        <i class="bi bi-clock-fill" style="font-size: 3rem; color: #00bcd4;"></i>
                        <h4 class="card-title fw-bold mt-3 text-dark">Horario</h4>
                    </div>
                </div>
            </a>
        </div>
        -->
    </div>
</div>
{% endblock %}
